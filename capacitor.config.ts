import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'idam-actividad6-21001135',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
